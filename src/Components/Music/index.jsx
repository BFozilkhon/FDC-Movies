import React from 'react';

export const Music = () => {
  // const dispatch = useDispatch();
  // useEffect(() => {
  // dispatch(musicApi);
  // }, []);

  return (
    <div>
      <h1>hey</h1>
    </div>
  );
};

export default Music;
